import { randomIdGenerator } from "../../../../../utility/Utils";

export const initialPackageTypeData = {
    name: "",
    note: '',
    sizes: [{
        id: randomIdGenerator(),
        value: []
    }]
};